# rust-zeroize-example
A very simple example to show the effectiveness of zeroize trait.

Run this code:
``` 
cargo run
```

You should be able to see if the memory is (automatically) zeroized or not.
